files = [
  'gt.gresource.xml',
  join_paths('gtk', 'menus.ui'),
  join_paths('ui', 'window.ui'),
  join_paths('theme', 'Adwaita.css')
]

install_data(
  files,
  install_dir: get_option('datadir')
)
